<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            font-family: "Montserrat";
        }
        .container {
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            text-align: center;
            margin-top: 50px; /* Adjust as needed */
        }
        .form_area {
            background-color: #EDDCD9;
            border: 2px solid #264143;
            border-radius: 20px;
            box-shadow: 3px 4px 0px 1px #E99F4C;
            padding: 20px; /* Adjust padding */
            max-width: 400px; /* Adjust as needed */
            text-align: center;
        }
        .title {
            color: #264143;
            font-weight: 900;
            font-size: 1.5em;
            margin-top: 20px;
        }
        .message {
            font-weight: 600;
            margin: 20px 0;
        }
        .link {
            font-weight: 800;
            color: #264143;
            padding: 5px;
            text-decoration: none;
        }
        .link:hover {
            text-decoration: underline;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Success</title>
</head>
<body>
    <div class="container">
        <div class="form_area">
            <p class="title">Login Successful!</p>
            <p class="message">Welcome back, <strong><%= userName %></strong>!</p>
            <p>Continue to <a class="link" href="/">Homepage</a></p>
        </div>
    </div>
</body>
</html>
